{
  "name": "gachangi-agent-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "가챙이 무인 에이전트 — Gmail '가계부' 라벨 → Drive → Gemini → Sheets (Cloudflare Cron Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
